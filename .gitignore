
# Plugin / Python
.venv*/
*.pyc
BambuCam.egg-info
*.swp
*.iml
build/
dist/
*.egg*
.DS_Store
*.zip
.ruff_cache/
.coverage
.pytest_cache/

# Docs (MkDocs build output + Node deps for JSDoc generation)
# package-lock.json IS committed so `npm ci` is reproducible in CI.
/site/
node_modules/

# Local-only tooling configs (not consumed by CI / pre-commit).
# Editors and local tools read these directly; the GitHub workflows do not.
.markdownlint.json
.markdownlintignore
.snyk
pyrightconfig.json
